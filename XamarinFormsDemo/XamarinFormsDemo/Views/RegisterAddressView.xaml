<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsDemo.Views.RegisterAddressView" Style="{StaticResource pageBaseStyle}">
  <StackLayout>
    <ContentView Padding="10,0,10,0" VerticalOptions="Start" HorizontalOptions="FillAndExpand">
      <Entry Text="{Binding Address}" Placeholder="送货地址" IsEnabled="True" Focused="VisualElement_OnFocused"/>
    </ContentView>
    <Button Text="重新选择" Command="{Binding SelectedAddressCommand}"></Button>
  </StackLayout>
</ContentPage>