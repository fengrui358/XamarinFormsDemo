<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsDemo.Views.SearchSuggestView">
  <StackLayout>
    <Entry VerticalOptions="Start" HorizontalOptions="FillAndExpand" Placeholder="搜索" Text="{Binding SearchKeyWord}" TextChanged="Entry_OnTextChanged"></Entry>
    <ListView ItemsSource="{Binding SuggestResults}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <Label Text="{Binding Name}"></Label>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>